apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tylerops-dev-tls
  namespace: envoy-gateway-system
spec:
  secretName: tylerops-dev-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.tylerops.dev"
